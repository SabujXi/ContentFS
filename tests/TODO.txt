# TODO: mtime returns float, json serialization deserialization (i guess) count up to some decimal point
#       devise the system to return only int or up to certain decimal point.
# DONE: TODO: CPathComponentsInfo <- instead of first char use 'drive' (windows drive or unix forward slash) and change other code accordingly.
